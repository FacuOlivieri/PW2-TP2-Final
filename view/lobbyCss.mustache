<style>
    .lobby-shell {
        overflow: hidden;
        padding: 0;
        border-color: rgba(212, 175, 55, 0.35);
    }

    .lobby-hero {
        position: relative;
        padding: 42px;
        background:
                radial-gradient(circle at 12% 20%, rgba(212, 175, 55, 0.26), transparent 30%),
                radial-gradient(circle at 85% 0%, rgba(192, 57, 43, 0.28), transparent 28%),
                linear-gradient(135deg, #2c2117 0%, #171717 56%, #21120f 100%);
        border-bottom: 1px solid rgba(212, 175, 55, 0.35);
    }

    .lobby-kicker {
        color: #d4af37;
        font-family: 'Cinzel', serif;
        font-size: 0.82em;
        font-weight: 700;
        letter-spacing: 2px;
        margin: 0 0 12px;
        text-transform: uppercase;
    }

    .lobby-title {
        color: #fff;
        font-size: clamp(2.1em, 6vw, 4.7em);
        line-height: 0.95;
        margin: 0;
        max-width: 720px;
        text-transform: none;
    }

    .lobby-title span {
        color: #d4af37;
        display: block;
    }

    .lobby-summary {
        display: grid;
        gap: 14px;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        margin-top: 34px;
    }

    .lobby-stat {
        background: rgba(255, 255, 255, 0.08);
        border: 1px solid rgba(255, 255, 255, 0.12);
        border-radius: 16px;
        padding: 18px;
        backdrop-filter: blur(8px);
    }

    .lobby-stat strong {
        color: #fff;
        display: block;
        font-family: 'Cinzel', serif;
        font-size: 1.75em;
        line-height: 1;
        margin-bottom: 8px;
    }

    .lobby-stat span {
        color: #cfcfcf;
        font-size: 0.86em;
        letter-spacing: 0.5px;
        text-transform: uppercase;
    }

    .lobby-actions {
        align-items: center;
        display: flex;
        flex-wrap: wrap;
        gap: 14px;
        margin-top: 30px;
    }

    .lobby-actions .btn-add,
    .lobby-actions .btn-secondary {
        margin: 0;
    }

    .lobby-actions button.btn-add {
        cursor: pointer;
        font-size: 1em;
    }

    .lobby-content {
        display: grid;
        gap: 24px;
        grid-template-columns: minmax(0, 1.05fr) minmax(310px, 0.95fr);
        padding: 30px;
    }

    .lobby-card {
        background: linear-gradient(180deg, rgba(51, 51, 51, 0.96), rgba(31, 31, 31, 0.96));
        border: 1px solid rgba(212, 175, 55, 0.28);
        border-radius: 18px;
        box-shadow: 0 14px 30px rgba(0, 0, 0, 0.28);
        overflow: hidden;
    }

    .lobby-card-header {
        align-items: flex-start;
        display: flex;
        gap: 16px;
        justify-content: space-between;
        padding: 22px 22px 14px;
    }

    .lobby-card-header h3 {
        color: #d4af37;
        font-family: 'Cinzel', serif;
        font-size: 1.28em;
        letter-spacing: 1px;
        margin: 0;
        text-transform: uppercase;
    }

    .lobby-card-header p {
        color: #a9a9a9;
        line-height: 1.5;
        margin: 8px 0 0;
    }

    .lobby-badge {
        background: rgba(192, 57, 43, 0.18);
        border: 1px solid rgba(192, 57, 43, 0.45);
        border-radius: 999px;
        color: #ffb3aa;
        font-size: 0.78em;
        font-weight: 700;
        padding: 8px 12px;
        text-transform: uppercase;
        white-space: nowrap;
    }

    .match-list {
        display: grid;
        gap: 12px;
        padding: 0 22px 22px;
    }

    .match-item {
        align-items: center;
        background: rgba(255, 255, 255, 0.045);
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: 14px;
        display: grid;
        gap: 14px;
        grid-template-columns: 1fr auto;
        padding: 16px;
    }

    .match-item h4 {
        color: #fff;
        margin: 0 0 6px;
    }

    .match-item p {
        color: #bdbdbd;
        margin: 0;
    }

    .match-result {
        border-radius: 999px;
        font-weight: 700;
        padding: 9px 12px;
        text-align: center;
        text-transform: uppercase;
    }

    .match-result.win {
        background: rgba(46, 204, 113, 0.14);
        color: #7bed9f;
    }

    .match-result.loss {
        background: rgba(231, 76, 60, 0.14);
        color: #ff7675;
    }

    .match-result.pending {
        background: rgba(212, 175, 55, 0.14);
        color: #f6d365;
    }

    .ranking-table {
        border: 0;
        border-top: 1px solid rgba(212, 175, 55, 0.28);
    }

    .ranking-table th,
    .ranking-table td {
        border-bottom: 1px solid rgba(255, 255, 255, 0.08);
        padding: 15px 18px;
    }

    .ranking-table th {
        background: rgba(0, 0, 0, 0.18);
    }

    .ranking-position {
        color: #d4af37;
        font-family: 'Cinzel', serif;
        font-weight: 700;
    }

    .empty-state {
        color: #bdbdbd;
        line-height: 1.6;
        margin: 0;
        padding: 0 22px 22px;
    }

    @media (max-width: 850px) {
        .lobby-hero,
        .lobby-content {
            padding: 24px;
        }

        .lobby-summary,
        .lobby-content {
            grid-template-columns: 1fr;
        }
    }

    @media (max-width: 520px) {
        .lobby-actions a,
        .lobby-actions button {
            box-sizing: border-box;
            text-align: center;
            width: 100%;
        }

        .match-item {
            align-items: flex-start;
            grid-template-columns: 1fr;
        }

        .ranking-table {
            font-size: 0.88em;
        }
    }
</style>