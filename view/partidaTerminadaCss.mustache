<style>

    .game-shell {
        overflow: hidden;
        padding: 0;
        border-color: rgba(212, 175, 55, 0.35);
    }

    .endgame-stage {
        padding: 40px;
        background:
                radial-gradient(circle at 20% 15%, rgba(192, 57, 43, 0.18), transparent 30%),
                radial-gradient(circle at 85% 10%, rgba(212, 175, 55, 0.16), transparent 28%),
                linear-gradient(135deg, #2b2116 0%, #181818 54%, #151f2a 100%);
    }

    .endgame-header {
        text-align: center;
        margin-bottom: 32px;
    }

    .result-badge {
        display: inline-block;
        background: rgba(192, 57, 43, 0.18);
        border: 1px solid rgba(192, 57, 43, 0.5);
        color: #ff9a8f;
        padding: 10px 18px;
        border-radius: 999px;
        font-weight: 700;
        letter-spacing: 1px;
        text-transform: uppercase;
        margin-bottom: 18px;
    }

    .endgame-header h1 {
        color: white;
        font-size: clamp(2rem, 4vw, 3.5rem);
        margin-bottom: 10px;
    }

    .endgame-message {
        color: #bdbdbd;
        font-size: 1.05rem;
    }

    .result-card {
        background: linear-gradient(
                180deg,
                rgba(47,47,47,0.96),
                rgba(26,26,26,0.98)
        );

        border-radius: 24px;
        padding: 35px;

        border: 1px solid rgba(255,255,255,0.08);

        box-shadow:
                0 20px 48px rgba(0,0,0,0.36);
    }

    .score-section {
        text-align: center;
        margin-bottom: 35px;
    }

    .score-label {
        display: block;
        color: #bdbdbd;
        margin-bottom: 8px;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .score-value {
        display: block;
        color: #d4af37;
        font-family: 'Cinzel', serif;
        font-size: 4rem;
        font-weight: bold;
    }

    .question-section h2,
    .question-section h3 {
        color: white;
        margin-bottom: 12px;
    }

    .question-box {
        background: rgba(255,255,255,0.05);
        border: 1px solid rgba(255,255,255,0.08);
        border-radius: 16px;
        padding: 20px;
        color: white;
        margin-bottom: 25px;
        font-size: 1.05rem;
    }

    .correct-answer-box {
        background: rgba(46, 204, 113, 0.12);
        border: 1px solid rgba(46, 204, 113, 0.35);
        border-radius: 16px;
        padding: 20px;
        color: #7fffb1;
        font-weight: 600;
    }

    .action-buttons {
        display: flex;
        justify-content: center;
        gap: 18px;
        margin-top: 35px;
        flex-wrap: wrap;
    }

    .primary-btn,
    .secondary-btn {
        text-decoration: none;
        padding: 14px 26px;
        border-radius: 14px;
        font-weight: 700;
        transition: all .2s ease;
    }

    .primary-btn {
        background: #d4af37;
        color: #1a1a1a;
    }

    .primary-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 24px rgba(212,175,55,0.35);
    }

    .secondary-btn {
        background: rgba(255,255,255,0.06);
        color: white;
        border: 1px solid rgba(255,255,255,0.12);
    }

    .secondary-btn:hover {
        background: rgba(255,255,255,0.10);
    }

    @media (max-width: 768px) {

        .endgame-stage {
            padding: 22px;
        }

        .result-card {
            padding: 24px;
        }

        .score-value {
            font-size: 3rem;
        }

        .action-buttons {
            flex-direction: column;
        }

        .primary-btn,
        .secondary-btn {
            text-align: center;
            width: 100%;
        }
    }

</style>