<style>
    .game-shell {
        overflow: hidden;
        padding: 0;
    }

    .endgame-stage {
        background:
                radial-gradient(circle at 18% 16%, rgba(245, 158, 11, 0.10), transparent 24%),
                linear-gradient(135deg, #ffffff 0%, #f5f3ff 58%, #eef2ff 100%);
        padding: clamp(22px, 4.5vw, 36px);
    }

    .endgame-header {
        margin-bottom: 26px;
        text-align: center;
    }

    .result-badge {
        background: var(--soft-amber);
        border: 1px solid #fde68a;
        border-radius: 999px;
        color: #b45309;
        display: inline-block;
        font-weight: 800;
        letter-spacing: 0.05em;
        margin-bottom: 16px;
        padding: 9px 16px;
        text-transform: uppercase;
    }

    .endgame-header h1 {
        color: var(--text);
        font-size: clamp(1.7rem, 3.6vw, 2.85rem);
        font-weight: 800;
        line-height: 1.1;
        margin: 0 0 10px;
    }

    .endgame-message {
        color: var(--muted);
        font-size: 1rem;
        line-height: 1.6;
        margin: 0;
    }

    .result-card {
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: var(--radius-lg);
        box-shadow: var(--shadow-md);
        overflow: hidden;
        padding: clamp(22px, 4.5vw, 32px);
        position: relative;
    }

    .result-card::before {
        background: linear-gradient(90deg, var(--primary), var(--secondary), var(--accent));
        content: "";
        height: 5px;
        left: 0;
        opacity: 0.75;
        position: absolute;
        right: 0;
        top: 0;
    }

    .score-section {
        margin-bottom: 28px;
        text-align: center;
    }

    .score-label {
        color: var(--muted);
        display: block;
        font-weight: 800;
        letter-spacing: 0.06em;
        margin-bottom: 8px;
        text-transform: uppercase;
    }

    .score-value {
        color: var(--primary);
        display: block;
        font-size: clamp(2.6rem, 7.2vw, 3.9rem);
        font-weight: 800;
        line-height: 1;
    }

    .question-section h2,
    .question-section h3 {
        color: var(--text);
        font-size: 1.05rem;
        font-weight: 800;
        margin: 0 0 12px;
    }

    .question-box,
    .correct-answer-box {
        border-radius: 18px;
        line-height: 1.6;
        margin-bottom: 20px;
        padding: 16px;
    }

    .question-box {
        background: #ffffff;
        border: 1px solid var(--border);
        color: var(--text);
    }

    .correct-answer-box {
        background: #ecfdf5;
        border: 1px solid #a7f3d0;
        color: var(--success);
        font-weight: 800;
        margin-bottom: 0;
    }

    .action-buttons {
        display: flex;
        flex-wrap: wrap;
        gap: 14px;
        justify-content: center;
        margin-top: 30px;
    }

    .report-question-form {
        margin: 20px 0 0;
        text-align: center;
    }

    .report-link-button {
        background: transparent;
        border: 1px solid #ddd6fe;
        border-radius: 999px;
        color: var(--primary-dark);
        cursor: pointer;
        font: inherit;
        font-size: 0.9rem;
        font-weight: 800;
        padding: 9px 16px;
    }

    .report-message {
        border-radius: var(--radius-sm);
        font-weight: 800;
        margin: 18px 0 0;
        padding: 12px 14px;
        text-align: center;
    }

    .report-success {
        background: #ecfdf5;
        border: 1px solid #a7f3d0;
        color: var(--success);
    }

    .report-error {
        background: #fef2f2;
        border: 1px solid #fecaca;
        color: var(--danger);
    }

    @media (max-width: 768px) {
        .action-buttons {
            flex-direction: column;
        }

        .primary-btn,
        .secondary-btn {
            text-align: center;
            width: 100%;
        }

    }
</style>
